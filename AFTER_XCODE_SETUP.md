# После установки Xcode

## 🎯 Быстрая настройка

После того, как Xcode установится из App Store, выполните следующие команды в терминале:

```bash
# 1. Перейдите в папку проекта
cd /Users/User/Development/elli_friends_app

# 2. Настройка Xcode
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
sudo xcodebuild -runFirstLaunch
sudo xcodebuild -license accept

# 3. Обновление CocoaPods
sudo gem install cocoapods

# 4. Проверка настройки
flutter doctor -v
```

## 🚀 Запуск приложения на macOS

После настройки Xcode:

```bash
# Запустить на macOS
flutter run -d macos

# Или в режиме релиза (быстрее)
flutter run -d macos --release
```

## 📱 Запуск на iOS симуляторе

```bash
# Посмотреть доступные устройства
flutter devices

# Запустить на iPhone симуляторе
flutter run -d iphone
```

## ✅ Преимущества нативной платформы

После настройки вы сможете:
- ✅ Компилировать приложения для macOS
- ✅ Компилировать приложения для iOS
- ✅ Использовать iOS симуляторы для тестирования
- ✅ Публиковать приложения в App Store
- ✅ Использовать все возможности Flutter без ограничений веб-платформы

## 🐛 Если что-то пошло не так

```bash
# Очистить кеш Flutter
flutter clean

# Переустановить зависимости
flutter pub get

# Проверить проблемы
flutter doctor -v
```

## 📞 Полезные команды

```bash
# Список всех симуляторов
xcrun simctl list devices

# Открыть Xcode
open /Applications/Xcode.app

# Обновить Flutter
flutter upgrade

# Собрать приложение без запуска
flutter build macos
flutter build ios
```

---

Когда всё будет готово, просто запустите:
```bash
flutter run -d macos
```

И ваше приложение запустится на macOS! 🎉
